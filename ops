{"author":{"id":"ff90e8c1032712f76e3c119e253ae3a84614b47da224d1a3450b4f469b0523a9"},"ops":[{"type":3,"timestamp":1585156259,"nonce":"lhD+pwFNxwy0BNUQmqcbOq060hA=","metadata":{"github-id":"MDEyOklzc3VlQ29tbWVudDYwMzk2Nzg2NA==","github-url":"https://github.com/svpcom/wfb-ng/issues/46#issuecomment-603967864"},"message":"No, you can use `/etc/wifibroadcast.cfg` to override parameters. `master.cfg` hold default values.\nAlso you can use different modulation for different streams (video, telemetry and IP tunnel)\nfor example:\n```\n[drone_video]\nbandwidth = 20     # bandwidth 20 or 40 MHz\nshort_gi = False   # use short GI or not\nstbc = 1           # stbc streams: 1, 2, 3 or 0 if unused\nldpc = 0\nmcs_index = 1      # mcs index\n\n[drone_mavlink]\nbandwidth = 20     # bandwidth 20 or 40 MHz\nshort_gi = False   # use short GI or not\nstbc = 1           # stbc streams: 1, 2, 3 or 0 if unused\nldpc = 0\nmcs_index = 1      # mcs index\n\n[gs_mavlink]\nbandwidth = 20     # bandwidth 20 or 40 MHz\nshort_gi = False   # use short GI or not\nstbc = 1           # stbc streams: 1, 2, 3 or 0 if unused\nldpc = 0\nmcs_index = 1      # mcs index\n\n[drone_tunnel]\nbandwidth = 20     # bandwidth 20 or 40 MHz\nshort_gi = False   # use short GI or not\nstbc = 1           # stbc streams: 1, 2, 3 or 0 if unused\nldpc = 0\nmcs_index = 1      # mcs index\n\n[gs_tunnel]\nbandwidth = 20     # bandwidth 20 or 40 MHz\nshort_gi = False   # use short GI or not\nstbc = 1           # stbc streams: 1, 2, 3 or 0 if unused\nldpc = 0\nmcs_index = 1      # mcs index\n```","files":null}]}